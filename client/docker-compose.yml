
version: '3'
services:
  backend:
    build:
      context: backend
    ports:
      - '8000:8000'
    networks:
      - opensearch-net
  frontend:
    build:
      context: frontend
    ports:
      - '8080:8080'
    networks:
      - opensearch-net

networks:
  opensearch-net: